import { ToolActivity } from "./ToolActivity";
import type { ActivityRenderClassItemProps } from "./types";

export function RelayOpActivity(props: ActivityRenderClassItemProps) {
  return <ToolActivity {...props} />;
}

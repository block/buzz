\* Drain-reachability probe (see module header of the Drain section).
\* INVERTED verdict: TLC reporting Probe_NotC VIOLATED = PASS (C reachable
\* from the stall state); TLC green = FAIL (migration deadlocks at the
\* C-gate). Mutant M10 removes MigrateB from DrainNext and must be green.
CONSTANTS
  Pods = {p1, p2, p3}
  None = None
  MaxGen = 4
SPECIFICATION DrainSpec
INVARIANTS
  Probe_NotC

#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/../../.." && pwd)"
TOOL_ROOT="$ROOT/tools/native-review"
. "$ROOT/bin/activate-hermit"
exec uv run --frozen --project "$TOOL_ROOT" python "$TOOL_ROOT/ios_review.py" "$@"

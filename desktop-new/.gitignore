node_modules
dist
playwright-report
test-results
*.log
.DS_Store
src/app/routeTree.gen.ts

# Local-only dev tooling. A config here wraps the committed vite.config.ts to
# add a personal plugin; it is ignored so it cannot be committed by accident.
vite.local.config.ts

# Isolated local runtime, private keys, and build products
.local/
